@model IEnumerable<DishModel>

<div class="cards-container first">
    <h1>Mejores Nigiris</h1>

    <div class="cards">
        @foreach (var dish in Model)
        {
            <partial name="_dishcard" model="dish" />
        }


    </div>

</div>